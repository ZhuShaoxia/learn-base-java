<service code="${code}" name="" label="${label}" serviceMethod="${serviceMethod}">
    <#--<describe>${describe}</describe>-->
    <request>
        <#--<#list fields as field>-->
            <#--<field name="${name}" label="${label}" type="${type}" length="${length}"/>-->
            <field name="${name}" label="${label}" type="${type}" length="${length}"/>
        <#--</#list>-->
    </request>
    <response>

    </response>
</service>